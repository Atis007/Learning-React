import ReactDOM from 'react-dom/client';

import App from './App.jsx';
import './index.css';

// this is the first code file that is executed when the website loads
// creating the root object and rendering the App component to the screen
ReactDOM.createRoot(document.getElementById('root')).render(<App />);
